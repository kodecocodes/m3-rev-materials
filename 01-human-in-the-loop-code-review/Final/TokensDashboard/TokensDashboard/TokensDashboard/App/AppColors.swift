/// Copyright (c) 2026 Kodeco Inc. See COPYRIGHT for details.
/// Caution: This is AI-generated code.

import SwiftUI

// MARK: - Design
// Light, editorial minimalism: serif numerals, hairline dividers, a single
// indigo accent. The summary reads as a narrative brief and drills into one graph screen: cost by model.

enum ColorStyles {
  static let shades: [Color] = [
    .indigo,
    .indigo.opacity(0.72),
    .indigo.opacity(0.52),
    .indigo.opacity(0.36),
    .indigo.opacity(0.22),
  ]
  /// Shared highlight color for whichever cohort or person needs
  /// attention, used consistently across every detail screen.
  static let attentionAccent = Color.orange
}
